<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="nfl.nflapp.main"
    android:background="@drawable/home">

    <Button
        android:layout_width="144dp"
        android:layout_height="52dp"
        android:id="@+id/setupBtn"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_gravity="left|bottom"
        android:onClick="setUpMessage"
        android:textColor="#ffffff"
        android:background="@drawable/setupbutton"
        android:textSize="15dp" />

    <Button
        android:layout_width="144dp"
        android:layout_height="52dp"
        android:id="@+id/instructionsBtn"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/mainTitle"
        android:layout_gravity="right|bottom"
        android:onClick="instructionsMessage"
        android:textColor="#ffffff"
        android:background="@drawable/instructbutton"
        android:textSize="15dp" />

</FrameLayout>
